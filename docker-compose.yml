
services:
  codelens:
    build: .
    container_name: codelens-rag
    ports:
      - "8501:8501"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY:-}
    volumes:
      - ./RAG/data:/app/RAG/data
    restart: unless-stopped
 